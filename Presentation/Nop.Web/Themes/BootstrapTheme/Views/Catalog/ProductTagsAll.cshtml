@model PopularProductTagsModel
@{
    Layout = "_ColumnsTwo";

    //title
    Html.AddTitleParts(T("PageTitle.AllProductTags").Text);
    //page class
    Html.AppendPageCssClassParts("html-product-tags-all-page");
}
<div class="page product-tags-all-page">
    <div class="page-title">
        <h2>@T("Products.Tags.All")</h2>
    </div>
    <div class="page-body">
        @if (Model.Tags.Count > 0)
        {
            <ul class="product-tags-list d-inline-flex flex-wrap">
                @foreach (var item in Model.Tags)
                {
                    <li class="mr-3 mb-3">
                        <a href="@Url.RouteUrl("ProductsByTag", new { item.SeName })" class="producttag btn btn-outline-secondary" style="font-size:@(Model.GetFontSize(item))%;">
                        @item.Name</a>
                    </li>
                }
            </ul>
        }
    </div>
</div>
