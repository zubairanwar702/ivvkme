@model Nop4you.Plugin.Widgets.SwiperSlider.Models.SlideConfigurationModel
@{
    Layout = "~/Areas/Admin/Views/Shared/_AdminPopupLayout.cshtml";
    //page title
    ViewBag.Title = T("nop4you.widget.SwiperSlider.AddNew").Text;
}


@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "slide-form" }))
{

    if (ViewBag.RefreshPage == true)
    {
        <script type="text/javascript">
            try {
                window.opener.document.forms[2].sldRefreshSlides.click();
            }
            catch (e) { }
            window.close();
        </script>
    }
    
    @await Html.PartialAsync("~/Plugins/Widgets.SwiperSlider/Views/WidgetsSwiperSlider/_CreateOrUpdate.cshtml", Model)
}